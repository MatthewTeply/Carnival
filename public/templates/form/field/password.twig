<div class="form-input-around col-lg-12">
    <input 
        type="text" 
        name="{{ name }}[value]" 
        class="form-control" 
        placeholder="{{ label }}"
        {% if attr.disabled %}
            disabled="disabled"
        {% endif %}
    >
</div>